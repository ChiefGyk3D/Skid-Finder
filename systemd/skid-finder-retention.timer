# Nightly retention: observations and captures are the bulk of the personal
# data and are only useful near the time (docs/data-handling.md).
[Unit]
Description=Skid Finder nightly retention

[Timer]
OnCalendar=daily
Persistent=true

[Install]
WantedBy=timers.target
